# The deterministic recovery demo requires no API keys and does not load an
# ambient .env file. Edit config/demo.toml for supported local settings.
# scripts/demo.py writes the exact run-scoped Compose environment to
# artifacts/<run-id>/compose.env, which is intentionally ignored by Git.
