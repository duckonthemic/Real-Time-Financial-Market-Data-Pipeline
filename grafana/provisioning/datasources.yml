apiVersion: 1

datasources:
  - name: Cassandra
    type: hadesarchitect-cassandra-datasource
    access: proxy
    url: cassandra:9042
    jsonData:
      keyspace: market_data
      user: cassandra
    secureJsonData:
      password: cassandra
    isDefault: true
